RewriteEngine On

# Serve existing files/directories directly (e.g. uploads)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else through the PHP router wrapper
RewriteRule ^ router.php [L,QSA]
